# smsflow-python Agent Instructions

This repository is intended to become the public Python SDK for SMSFlow.

- Do not commit real SMSFlow credentials, phone numbers, tokens, customer data, or package publishing tokens.
- Keep SDK code dependency-light and compatible with common backend Python applications.
- Use `.env.example` only for placeholder local configuration.
